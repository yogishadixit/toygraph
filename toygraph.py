class ToyGraph:
  """A simple implementation of graph manipulation software"""

  	def __init__(self):
  	  self.nodes = []
  	  self.edges = []

  	def add_edge(a, b):
  	  if nodes.count(a) == 0:
  	    nodes.append(a)
  	  if nodes.count(b) == 0:
  	    nodes.append(b)
  	  edges.append(a,b)

  	def add_edges(x):
  	  for edge in x:
  	    add_edge(edge)

  	